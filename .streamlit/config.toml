[theme]
primaryColor = "#475569"
backgroundColor = "#F8FAFC"
secondaryBackgroundColor = "#F1F5F9"
textColor = "#0F172A"
font = "sans serif"

[server]
maxUploadSize = 200  # Limit uploads to 200MB to prevent OOM / API timeouts
